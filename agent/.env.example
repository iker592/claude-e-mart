# Claude Agent SDK Configuration
# Copy this file to .env and configure your preferred provider

# =============================================================================
# PROVIDER SELECTION
# =============================================================================
# The Claude Agent SDK supports multiple providers. Set ONE of these to enable:
#
# Option 1: Direct Anthropic API (default)
#   - Set ANTHROPIC_API_KEY
#   - Leave CLAUDE_CODE_USE_BEDROCK and CLAUDE_CODE_USE_VERTEX unset
#
# Option 2: AWS Bedrock
#   - Set CLAUDE_CODE_USE_BEDROCK=1
#   - Configure AWS credentials (see below)
#
# Option 3: Google Vertex AI
#   - Set CLAUDE_CODE_USE_VERTEX=1
#   - Configure GCP credentials

# =============================================================================
# DIRECT ANTHROPIC API CONFIGURATION
# =============================================================================
# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# =============================================================================
# AWS BEDROCK CONFIGURATION
# =============================================================================
# Enable Bedrock as the LLM provider
# CLAUDE_CODE_USE_BEDROCK=1

# AWS Region where Bedrock is available with Claude models
# Required regions: us-east-1, us-west-2, eu-west-1, ap-northeast-1
# AWS_REGION=us-east-1

# AWS Credentials (choose one method):
#
# Method 1: Explicit credentials (not recommended for production)
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
#
# Method 2: AWS Profile (recommended for local development)
# AWS_PROFILE=your-profile-name
#
# Method 3: IAM Role (recommended for EC2/Lambda/ECS)
# - No environment variables needed
# - The SDK will automatically use instance profile or task role credentials

# Optional: Bedrock-specific settings
# BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0
# BEDROCK_ENDPOINT_URL=https://bedrock-runtime.us-east-1.amazonaws.com

# =============================================================================
# GOOGLE VERTEX AI CONFIGURATION
# =============================================================================
# Enable Vertex AI as the LLM provider
# CLAUDE_CODE_USE_VERTEX=1

# GCP Project ID
# GOOGLE_CLOUD_PROJECT=your-project-id

# GCP Region
# GOOGLE_CLOUD_REGION=us-central1

# =============================================================================
# SESSION STORAGE (OPTIONAL)
# =============================================================================
# By default, sessions are stored locally in ~/.claude/projects/
# Set SESSION_BUCKET_NAME to use S3 for session storage (recommended for production)
#
# SESSION_BUCKET_NAME=my-claude-sessions-bucket
# SESSION_BUCKET_PREFIX=sessions/
#
# Note: S3 storage uses the same AWS credentials configured above for Bedrock

# =============================================================================
# SERVER CONFIGURATION (OPTIONAL)
# =============================================================================
# CORS origins for the FastAPI server (comma-separated)
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Environment name (affects logging level and CORS policies)
# ENV_NAME=dev

# =============================================================================
# OPENTELEMETRY TRACING (OPTIONAL)
# =============================================================================
# OTLP exporter endpoint for Jaeger or other collectors
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
# OTEL_SERVICE_NAME=claude-e-mart-agent
