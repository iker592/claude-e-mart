# UI Environment Configuration
# Copy to .env.local for local development

# API URL - leave empty to use proxy or same-origin in production
# For local development with separate backend:
VITE_API_URL=http://localhost:8000

# For production, CloudFront handles routing to the API
# VITE_API_URL=
